# --- CalcIng Frontend ---
# Copiar este archivo como .env y ajustar los valores.

# URL base del backend (sin trailing slash)
VITE_API_URL=http://localhost:8001

# Activar backend SymPy (true = sympyAdapter, false = nerdamer offline)
VITE_USE_BACKEND=false

# Stripe Price IDs (públicos, no son secrets)
VITE_STRIPE_PRICE_PRO_MONTHLY=
VITE_STRIPE_PRICE_PRO_ANNUAL=
VITE_STRIPE_PRICE_ENTERPRISE_MONTHLY=
VITE_STRIPE_PRICE_ENTERPRISE_ANNUAL=
